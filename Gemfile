# frozen_string_literal: true

source "https://rubygems.org"

# gem "rails"
# gem 'jekyll-theme-chirpy', '~> 6.4', '>= 6.4.2'
# ^^ I dont like using someone elses theme it seems especially one not 
# designed to be used as a gem but expected to be used as a template

gem "jekyll"
gem 'wdm', '>= 0.1.0' if Gem.win_platform? #To allow monitoring of windows dirs

group :jekyll_plugins do
  gem "jekyll-sitemap"
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
end
